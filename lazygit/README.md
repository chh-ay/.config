# For side by side comparison do in your respective shell(fish, zsh, bash)

```fish
set --export DELTA_FEATURES side-by-side #activate
```

```shell
export DELTA_FEATURES=+side-by-side # activate
export DELTA_FEATURES=+             # deactivate
```
